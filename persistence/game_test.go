package persistence

import "testing"

func TestGameInit(t *testing.T) {
	// g := NewGame()
	// r := fmt.Sprintf("%v", reflect.TypeOf(g))
	// if r != "persistence.Game" {
	// t.Error(r, "is not a Game")
	// }
	// if g. != "persistence.Game" {
	// t.Error(r, "is not a Game")
	// }
}
